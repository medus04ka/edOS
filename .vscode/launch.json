{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/bin/app",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build All"
        }
    ]
}